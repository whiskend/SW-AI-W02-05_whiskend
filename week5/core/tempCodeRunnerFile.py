dp[0][0] = triangle[0][0]
        # dp[1][0] = triangle[1][0]
        # dp[1][1] = triangle[1][1]

        # for n in range(size):
        #     for i in range(0, n):
        #         dp[n][i] = min(dp[n-1][i]+triangle[n][i], dp[n-1][i-1]+triangle[n][i])

        # return min(dp[n])